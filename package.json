{
  "name": "landing-pages-armazem-gasolina",
  "version": "0.1.0",
  "private": true,
  "description": "Landing pages estáticas da Armazém Auto Peças — veículos a gasolina (Lifan e linha asiática). Sem consulta por placa, captação ampla via WhatsApp.",
  "scripts": {
    "dev": "next dev",
    "build": "node scripts/validate-configs.mjs && next build && node scripts/verify-build.mjs",
    "start": "next start",
    "verify": "node scripts/verify-build.mjs",
    "validate-configs": "node scripts/validate-configs.mjs"
  },
  "dependencies": {
    "next": "^16.2.4",
    "react": "^19.2.5",
    "react-dom": "^19.2.5"
  },
  "devDependencies": {
    "ajv": "^8.20.0"
  }
}
